# 保质期管理系统部署指南

**项目名称**: 保质期管理系统
**文档状态**: 🟢 进行中
**适用版本**: v2.13.1

---

## 部署环境要求

### 硬件要求
- 最低配置: 1核CPU, 1GB内存, 10GB磁盘空间
- 推荐配置: 2核CPU, 2GB内存, 20GB磁盘空间

### 软件要求
- 操作系统: Linux (CentOS 7 或 Ubuntu 18.04+)
- 服务器软件: Nginx 1.16+ 或 Apache 2.4+
- PHP 版本: 7.4+ (推荐 PHP 8.0+)
- MySQL 版本: 5.7+ (推荐 MySQL 8.0)

---

## 部署步骤

### 第一步: 上传文件到服务器

#### 使用 FTP 上传
1. 连接到服务器
2. 创建网站根目录 (例如: /var/www/html/pandian)
3. 上传项目文件到该目录
4. 设置文件权限为 755

#### 使用 SSH 上传
```bash
cd /var/www/html
mkdir pandian
cd pandian
wget https://github.com/your-username/expiry-management-system/archive/v2.13.1.zip
unzip v2.13.1.zip
mv expiry-management-system-2.13.1/* .
rm -rf expiry-management-system-2.13.1
```

---

### 第二步: 配置数据库连接

1. 复制配置文件
   ```bash
   cp config.sample.php config.php
   ```

2. 编辑配置文件
   ```php
   <?php
   $db_host = "localhost";
   $db_name = "pandian";
   $db_user = "root";
   $db_pass = "password";
   ?>
   ```

---

### 第三步: 导入数据库结构

1. 创建数据库
   ```sql
   CREATE DATABASE pandian CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

2. 导入数据库结构
   ```bash
   mysql -u root -p pandian < database.sql
   ```

---

### 第四步: 配置虚拟主机

#### Nginx 配置
```nginx
server {
    listen 80;
    server_name pandian.dhmip.cn;
    root /var/www/html/pandian;
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php7.4-fpm.sock;
    }

    location ~ /\.ht {
        deny all;
    }
}
```

#### Apache 配置
```apache
<VirtualHost *:80>
    ServerName pandian.dhmip.cn
    DocumentRoot /var/www/html/pandian
    ErrorLog ${APACHE_LOG_DIR}/pandian_error.log
    CustomLog ${APACHE_LOG_DIR}/pandian_access.log combined

    <Directory /var/www/html/pandian>
        Options Indexes FollowSymLinks MultiViews
        AllowOverride All
        Require all granted
        Order allow,deny
        allow from all
    </Directory>
</VirtualHost>
```

---

### 第五步: 访问网站进行安装

1. 在浏览器中访问:
   ```
   http://pandian.dhmip.cn
   ```

2. 按照页面提示完成安装

---

## 安全配置

### 1. 文件权限设置
```bash
chmod -R 755 /var/www/html/pandian
chmod 600 /var/www/html/pandian/config.php
```

### 2. 数据库安全
- 不要使用 root 用户直接连接数据库
- 为数据库创建专门的用户
- 定期备份数据库

### 3. 防火墙配置
```bash
# 允许 HTTP 和 HTTPS 访问
firewall-cmd --permanent --add-service=http
firewall-cmd --permanent --add-service=https
firewall-cmd --reload
```

---

## 性能优化

### 1. PHP 配置优化
```ini
; php.ini 配置
memory_limit = 512M
upload_max_filesize = 100M
post_max_size = 100M
max_execution_time = 300
```

### 2. MySQL 配置优化
```ini
; my.cnf 配置
innodb_buffer_pool_size = 2G
query_cache_size = 128M
max_connections = 200
```

---

## 备份策略

### 1. 数据库备份
```bash
# 每日备份
mysqldump -u root -p pandian > /backup/pandian_$(date +%Y%m%d).sql
```

### 2. 文件备份
```bash
# 每日备份
tar -czf /backup/pandian_files_$(date +%Y%m%d).tar.gz /var/www/html/pandian
```

---

## 恢复方法

### 1. 数据库恢复
```bash
mysql -u root -p pandian < /backup/pandian_20260221.sql
```

### 2. 文件恢复
```bash
rm -rf /var/www/html/pandian
mkdir /var/www/html/pandian
tar -xzf /backup/pandian_files_20260221.tar.gz -C /var/www/html/pandian
```

---

## 常见问题

### 1. 无法访问网站
- 检查服务器是否运行
- 检查防火墙设置
- 检查虚拟主机配置

### 2. MySQL 连接失败
- 检查数据库连接配置
- 检查数据库服务是否运行
- 检查用户权限

### 3. 文件上传失败
- 检查临时目录权限
- 检查上传文件大小限制
- 检查磁盘空间

---

## 相关链接

[[保质期管理系统开发日志]]
[[保质期管理系统API文档]]
