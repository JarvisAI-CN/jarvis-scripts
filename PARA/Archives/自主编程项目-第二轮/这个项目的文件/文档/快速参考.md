# 自主编程系统 v2.0 - 快速参考

**版本**: 2.0 (GLM-5 Enhanced)
**更新时间**: 2026-02-13

---

## 🚀 三分钟上手

### 1. 同步 TODO.md 到任务列表

```bash
cd /home/ubuntu/.openclaw/workspace
python3 scripts/todo_to_tasklist.py sync
```

**输出**:
```
✅ 解析到 8 个待处理任务
✅ 添加任务: TASK-2026-02-13-001 - 自主编程项目-第二轮
...
```

### 2. 查看任务列表

```bash
python3 scripts/todo_to_tasklist.py list
```

**输出**:
```
📝 所有任务 (8):

⏳ [TASK-2026-02-13-001] 自主编程项目-第二轮
   类型: feature | 优先级: high | 状态: pending
⏳ [TASK-2026-02-13-002] ImageHub技术分享项目
   类型: feature | 优先级: medium | 状态: pending
...
```

### 3. 启动自主控制器

```bash
python3 scripts/autonomous_controller.py run
```

**后台运行**:
```bash
nohup python3 scripts/autonomous_controller.py run > /dev/null 2>&1 &
```

### 4. 监控执行日志

```bash
tail -f logs/progress_flow.log
```

**输出**:
```
[2026-02-13 17:10:00.123] [TASK-MANAGER] [INFO] Task started
[2026-02-13 17:10:05.456] [GIT-OPERATOR] [SUCCESS] Commit a1b2c3d
[2026-02-13 17:10:10.789] [E2E-VERIFIER] [SUCCESS] All tests passed
```

---

## 📋 常用命令

### todo_to_tasklist.py

```bash
# 同步 TODO.md
python3 scripts/todo_to_tasklist.py sync

# 查看统计
python3 scripts/todo_to_tasklist.py stats

# 列出所有任务
python3 scripts/todo_to_tasklist.py list
```

### autonomous_controller.py

```bash
# 运行主循环
python3 scripts/autonomous_controller.py run

# 测试模式
python3 scripts/autonomous_controller.py test
```

### auto_maintain_v2.sh

```bash
# 完整维护流程
./scripts/auto_maintain_v2.sh run

# 仅健康检查
./scripts/auto_maintain_v2.sh check

# 强制修复特定项目
./scripts/auto_maintain_v2.sh fix webdav
./scripts/auto_maintain_v2.sh fix gateway

# 事件驱动模式
./scripts/auto_maintain_v2.sh event
```

---

## 📊 核心文件

| 文件 | 用途 |
|-----|------|
| `.task_list.json` | 任务列表（机器可读） |
| `logs/progress_flow.log` | 执行日志（实时） |
| `TODO.md` | 任务定义（人类可读） |
| `config/autonomous_config.yaml` | 配置文件 |

---

## 🔄 完整工作流

```
1. 编辑 TODO.md (添加新任务)
   ↓
2. python3 scripts/todo_to_tasklist.py sync
   ↓
3. python3 scripts/autonomous_controller.py run
   ↓
4. tail -f logs/progress_flow.log (监控)
   ↓
5. 任务完成，Git 自动提交
```

---

## 🎯 任务状态说明

| 状态 | 图标 | 说明 |
|-----|------|------|
| pending | ⏳ | 待处理 |
| in_progress | 🔄 | 进行中 |
| done | ✅ | 已完成 |
| failed | ❌ | 失败 |
| cancelled | ⏸️ | 已取消 |

---

## 🔧 配置编辑

编辑 `config/autonomous_config.yaml`:

```yaml
# 上下文压缩
context:
  compress_every: 5  # 每 5 个任务
  compress_minutes: 30  # 或每 30 分钟

# Git 配置
git:
  auto_push: true  # 自动推送
  branch: main

# 调度配置
scheduling:
  mode: event_driven  # 事件驱动模式
```

---

## 📈 监控指标

```bash
# 查看任务统计
python3 scripts/todo_to_tasklist.py stats

# 查看最新提交
git log --oneline -10

# 查看任务历史
cat .task_list.json | jq '.tasks[] | {id, title, status}'
```

---

## 🐛 故障排除

### 问题：TODO.md 解析失败

**解决**:
```bash
# 检查格式
cat TODO.md | head -20

# 重新同步
rm -f .task_list.json
python3 scripts/todo_to_tasklist.py sync
```

### 问题：Git 提交失败

**解决**:
```bash
# 检查 Git 状态
git status

# 手动提交
git add .
git commit -m "manual commit"
git push
```

### 问题：控制器卡住

**解决**:
```bash
# 查看日志
tail -f logs/progress_flow.log

# 重启控制器
pkill -f autonomous_controller.py
nohup python3 scripts/autonomous_controller.py run > /dev/null 2>&1 &
```

---

## 💡 高级用法

### 手动添加任务

编辑 `.task_list.json`:

```json
{
  "id": "TASK-2026-02-13-999",
  "title": "手动任务",
  "type": "feature",
  "priority": "high",
  "status": "pending",
  "created_at": "2026-02-13T17:00:00",
  ...
}
```

### 强制执行特定任务

```python
# 在 Python 中
from scripts.autonomous_controller import AutonomousController

controller = AutonomousController()
task = controller.task_manager.get_task("TASK-2026-02-13-001")
controller.execute_task(task)
```

### 查看上下文历史

```bash
cat .maintenance_state/context.json | jq '.compressed_contexts'
```

---

## 📚 相关文档

- 📖 [完整设计方案](这个项目的文件/文档/设计方案.md)
- 📊 [实施报告](这个项目的文件/文档/实施报告-2026-02-13.md)
- 🔧 [维护脚本文档](scripts/README.md)

---

## 🎓 核心概念

### task_list.json
机器可执行的任务列表，包含:
- 任务 ID、标题、描述
- 类型、优先级、状态
- 子任务、日志、Git 提交

### progress_flow.log
执行日志，格式:
```
[时间戳] [模块名] [级别] 消息
```

### Commit-per-Task
每个子任务一个 Git 提交，支持精确回滚

### 上下文压缩
长时间运行时定期压缩，保留关键信息

### E2E 验证
四层验证确保工作质量

---

## 🚦 系统状态

查看当前系统状态:

```bash
# 健康检查
./scripts/auto_maintain_v2.sh check

# 任务统计
python3 scripts/todo_to_tasklist.py stats

# Git 状态
git status

# 日志大小
du -sh logs/
```

---

## 📞 帮助与支持

**问题反馈**:
- 编辑 `PARA/Projects/自主编程项目-第二轮/README.md`
- 或创建 issue

**日志位置**:
- 主日志: `logs/progress_flow.log`
- 维护日志: `logs/auto_maintain_v2.log`

---

**最后更新**: 2026-02-13
**维护者**: GLM-4.7 (贾维斯)
