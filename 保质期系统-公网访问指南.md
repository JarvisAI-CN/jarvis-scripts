# 保质期管理系统 - 公网访问配置

**部署时间**: 2026-02-17 11:33
**访问地址**: http://150.109.204.23:8081

---

## ✅ 已配置

### Nginx配置
- **监听地址**: 0.0.0.0:8081（所有接口）
- **内网访问**: http://10.7.0.5:8081 ✅
- **公网访问**: http://150.109.204.23:8081 ✅

### 登录信息
- **用户名**: admin
- **密码**: admin123

---

## 🔐 安全建议

1. **修改默认密码**
   - 登录后立即修改admin密码
   - 使用强密码（大小写+数字+符号）

2. **限制访问IP**（可选）
   如果只想让特定IP访问，可以配置Nginx：

   ```nginx
   location / {
       # 只允许特定IP
       allow 1.2.3.4;  # 替换为你的IP
       deny all;
       
       try_files $uri $uri/ =404;
   }
   ```

3. **启用HTTPS**（推荐）
   - 使用Let's Encrypt免费证书
   - 或使用自签名证书

4. **定期备份数据库**
   ```bash
   mysqldump expiry_system > backup_$(date +%Y%m%d).sql
   ```

---

## 📱 移动端访问

### 通过公网
- **URL**: http://150.109.204.23:8081
- 适用于手机浏览器
- 支持扫码功能

### 局域网访问
- **URL**: http://10.7.0.5:8081
- 适用于同一网络内的设备
- 速度更快

---

## 🛠️ 管理命令

### 重启服务
```bash
# 重启Nginx
sudo systemctl restart nginx

# 重启PHP
sudo systemctl restart php8.3-fpm

# 查看日志
sudo tail -f /var/log/nginx/expiry-error.log
```

### 查看状态
```bash
# 检查服务状态
sudo systemctl status nginx
sudo systemctl status php8.3-fpm

# 检查端口监听
sudo netstat -tlnp | grep 8081
```

---

## ⚠️ 注意事项

1. **公网暴露风险**: 系统现在可以通过公网访问，请确保密码安全
2. **数据备份**: 定期备份重要数据
3. **日志监控**: 定期检查访问日志，发现异常及时处理

---

**系统已就绪，可以通过公网IP访问！** 🚀
