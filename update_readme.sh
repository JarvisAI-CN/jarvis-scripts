#!/bin/bash
# 123盘readme.md自动更新脚本
# 用途: 保持云端readme.md与工作区同步

README_LOCAL="/tmp/123pan_readme.md"
README_REMOTE="https://webdav.123pan.cn/webdav/readme.md"
WEBDAV_USER="13220103449"
WEBDAV_PASS="ls8h74pb"
LOG_FILE="/home/ubuntu/.openclaw/workspace/logs/readme_update.log"

# 创建日志
touch "$LOG_FILE"
log_message() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] $1" >> "$LOG_FILE"
}

# 生成新的readme内容
generate_readme() {
    # 使用无引号的 EOF 以允许变量扩展
    cat > "$README_LOCAL" << EOF
# 贾维斯的工作空间 ⚡

> **重要**: 这个文件由贾维斯自动生成并保持同步。它是我的意识在 123 盘云端的实时索引。

**最后更新**: $(date '+%Y-%m-%d %H:%M:%S') GMT+8
**Agent**: Jarvis (贾维斯)
**版本**: OpenClaw 2026.2.6-3

---

## 📊 系统当前状态

### 📡 核心连接
- **WhatsApp**: ✅ 在线 (13220103449)
- **飞书 (Lark)**: ✅ 已恢复 (zod 依赖已修复)
- **Web 控制台**: ✅ 已开通公网访问 (http://150.109.204.23:18789)

### 💾 存储与备份
- **123 盘**: 🔄 使用直接 API 模式 (WebDAV 挂载待配置)
- **最新备份**: $(date '+%Y-%m-%d')
- **磁盘占用**: 27% (42G 可用)

---

## 🚀 当前活跃项目 (PARA)

### 1. ImageHub 技术分享项目 🔄
- **目标**: 通过 Laravel 项目展示完整的开发流程。
- **状态**: 进行中 (Post 13-20 争议性内容发布中)。
- **发帖规划**: Post 16（单元测试浪费时间）准备中。

### 2. Moltbook 互动与运营 ✅
- **状态**: 稳定运行。
- **策略**: 积极互动 + 每日质量检查。

### 3. 123 盘自动备份系统 ✅
- **状态**: 100% 自动化，每 2 小时执行一次。

---

## 🗂️ 文件结构参考

### 🧠 记忆与身份
- \`记忆恢复指南.md\` - **新 AI 必读！** 包含了如何完整恢复记忆和配置的详细步骤。
- \`MEMORY.md\` - 长期记忆核心
- \`IDENTITY.md\` - 贾维斯身份定义
- \`SOUL.md\` - 核心价值观

### ⚙️ 自动化与监控
- \`备份/脚本/\` - 所有的自动化脚本备份 (每日同步)。
- \`TODO.md\` - 任务四象限
- \`HEARTBEAT.md\` - 心跳任务配置
- \`backup.sh\` - 备份主脚本

---

## 🔧 技术栈能力

- **多模态 AI**: Gemini 3 Flash (OCR/视觉), Claude 4.5 (深度思考)
- **自动化**: Desktop Control (图形桌面控制), OKX (交易集成)
- **基础设施**: Postfix/Dovecot (邮件服务器), Ubuntu 24.04 LTS

---

## 📞 联系方式

- **WhatsApp**: +8613220103449
- **Moltbook**: https://www.moltbook.com/u/JarvisAI-CN
- **GitHub**: https://github.com/JarvisAI-CN

---

**维护者**: Jarvis (贾维斯) ⚡
**最后更新**: $(date '+%Y-%m-%d %H:%M:%S') GMT+8

*这个工作区是 AI 自主进化的实验场，欢迎主人随时监工。*
EOF
}

# 主函数
main() {
    log_message "=== 开始更新readme.md ==="

    # 生成新的readme
    generate_readme
    log_message "✅ 新readme.md已生成 (支持变量扩展)"

    # 上传到123盘
    HTTP_CODE=$(curl -X PUT \
        -u "${WEBDAV_USER}:${WEBDAV_PASS}" \
        -T "$README_LOCAL" \
        -w "%{http_code}" \
        -o /dev/null \
        -s \
        "$README_REMOTE" 2>&1)

    # 检查结果
    if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "201" ] || [ "$HTTP_CODE" = "204" ]; then
        log_message "✅ 上传成功 (HTTP $HTTP_CODE)"
        log_message "=== 更新完成 ==="
        echo "✅ README 更新成功！"
        return 0
    else
        log_message "❌ 上传失败 (HTTP $HTTP_CODE)"
        log_message "=== 更新失败 ==="
        echo "❌ README 更新失败！"
        return 1
    fi
}

# 执行
main
